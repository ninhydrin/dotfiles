# Claude Code Hook 設定ファイル
# このファイルを `.env` にコピーして使用してください

# ====================
# 必須設定
# ====================

# Slack API トークン（Bot Token）
# Slack App の OAuth & Permissions から取得
SLACK_API_TOKEN=xoxb-your-slack-bot-token-here

# Slack チャンネル ID
# チャンネルを右クリック → リンクをコピー → URL末尾の ID
CHANNEL_ID=C1234567890


# ====================
# オプション設定
# ====================

# 通知のグローバル制御（true/false）
# falseに設定すると、対応する通知が完全に無効化されます

# Slack通知を有効にするか（デフォルト: true）
# GLOBAL_ENABLE_SLACK_NOTIFICATION=true

# AppleScript通知（macOS通知センター）を有効にするか（デフォルト: true）
# GLOBAL_ENABLE_APPLESCRIPT_NOTIFICATION=true

# 音声通知を有効にするか（デフォルト: true）
# GLOBAL_ENABLE_SOUND_NOTIFICATION=true

# デバッグログを有効にするか（デフォルト: false）
# ENABLE_DEBUG_LOG=true

# デバッグログファイルのパス（デフォルト: ~/.claude/hook_debug.log）
# DEBUG_LOG_FILE=~/.claude/hook_debug.log
# 音声通知のディレクトリ（デフォルト: ~/dataset/claude/notifications）
# SOUND_DIR=~/dataset/claude/notifications

# 音声通知の音量（0.0〜1.0、デフォルト: 0.5）
# SOUND_VOLUME=0.5

# Bashコマンドセキュリティチェックを有効にするか（デフォルト: true）
# ENABLE_BASH_SECURITY_CHECK=true
