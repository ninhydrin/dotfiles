bindkey '^[[109;5u' accept-line          # Ctrl+M → Enter
bindkey '^[[105;5u' expand-or-complete   # Ctrl+I → Tab補完
bindkey '^[[104;5u' backward-delete-char # Ctrl+H → Backspace
bindkey '^[[106;5u' accept-line          # Ctrl+J → Enter（Line Feed）
